import * as React from 'react';
import { PageTemplate, PanelsMain } from 'component';

const Panels: React.FC = () => (
  <div>
    <PageTemplate>
      <PanelsMain />
    </PageTemplate>
  </div>
);

export default Panels;