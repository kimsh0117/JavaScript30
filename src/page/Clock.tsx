import * as React from 'react';
import { PageTemplate, ClockMain } from 'component';

const Clock: React.FC = () => (
  <div>
    <PageTemplate>
      <ClockMain />
    </PageTemplate>
  </div>
);

export default Clock;