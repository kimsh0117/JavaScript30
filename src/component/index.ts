export { default as App } from 'component/App';

// common
export { default as PageTemplate } from 'component/common/PageTemplate';

// drum-kit
export { DrumMain } from 'component/drum-kit';

// css + clock
export { ClockMain } from 'component/clock';
